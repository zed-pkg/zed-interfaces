{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "PackageMetadata",
  "type": "object",
  "properties": {
    "description": {
      "type": [
        "string",
        "null"
      ]
    },
    "latest": {
      "type": [
        "string",
        "null"
      ]
    },
    "mirrors": {
      "description": "Where this package's artifacts and metadata can be fetched when this\nregistry cannot answer, in try order.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/MirrorDescriptorV1"
      }
    },
    "name": {
      "type": "string"
    },
    "org": {
      "type": "string"
    },
    "repo_url": {
      "type": "string"
    },
    "signing_keys": {
      "description": "The org's publisher signing keys, inlined so a client that resolves a\npackage also learns how to verify a mirror's answer next time — without\na second round trip it may not get to make.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/PublisherKeyV1"
      }
    },
    "tags": {
      "description": "Free-form tags for filtering/discovery (multi-tag lookup).",
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "vcs": {
      "$ref": "#/$defs/Vcs"
    },
    "version_scheme": {
      "description": "How this package's versions should be interpreted (semver by default).",
      "$ref": "#/$defs/VersionScheme"
    },
    "versions": {
      "description": "All published, non-yanked versions, newest first.",
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  },
  "required": [
    "org",
    "name",
    "vcs",
    "repo_url",
    "versions"
  ],
  "$defs": {
    "MirrorDescriptorV1": {
      "description": "One place a package's artifacts and/or metadata can be fetched from.\n\nAuthors write the shortest form that identifies the mirror and the derived\ndefaults fill in the rest — a `github-release` mirror with no fields at all\nmeans \"the release assets on my own `[package.repository]`\".",
      "type": "object",
      "properties": {
        "alternate_urls": {
          "description": "Additional base URLs serving byte-identical content at a different\nhostname.\n\nThis is the field that survives a zone-level outage. `cdn.zpkg.net` and\na `workers.dev` hostname front the same bucket, but they fail\nindependently, because the second does not resolve through the\n`zpkg.net` zone at all. A client tries every base URL of a mirror\nbefore moving to the next mirror.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "artifact_template": {
          "description": "Artifact key template. Only for a store whose layout is not\n[`DEFAULT_ARTIFACT_TEMPLATE`] — a sharded bucket, or one where zed's\nobjects live under a prefix shared with other tooling.",
          "type": [
            "string",
            "null"
          ]
        },
        "asset_prefix": {
          "description": "Prefix on zed-owned release asset names. Defaults to [`DEFAULT_ASSET_PREFIX`].",
          "type": [
            "string",
            "null"
          ]
        },
        "branch": {
          "description": "Branch of a raw-served mirror tree. Defaults to [`DEFAULT_RAW_BRANCH`].",
          "type": [
            "string",
            "null"
          ]
        },
        "id": {
          "description": "Stable identifier, used in diagnostics and to deduplicate a merged\nmirror set. Derived from the kind and host when absent.",
          "type": [
            "string",
            "null"
          ]
        },
        "index_tag": {
          "description": "Tag of the rolling release carrying package indexes. Defaults to\n[`DEFAULT_INDEX_TAG`].",
          "type": [
            "string",
            "null"
          ]
        },
        "index_template": {
          "description": "Package-index key template; see [`DEFAULT_INDEX_TEMPLATE`].",
          "type": [
            "string",
            "null"
          ]
        },
        "kind": {
          "$ref": "#/$defs/MirrorKindV1"
        },
        "oci_repository": {
          "description": "Repository path *inside* an OCI registry, when it is not the forge\nrepository's own `owner/name`.\n\nThe default is deliberate: pushing `github.com/acme/http-kit` to\n`ghcr.io/acme/http-kit` is what makes GitHub attach the package to that\nrepository. Override it when one repository publishes several packages\n(`acme/http-kit/client-rust`) or when the registry namespace simply is\nnot the repository name.",
          "type": [
            "string",
            "null"
          ]
        },
        "path": {
          "description": "Absolute local path, for `directory` mirrors.",
          "type": [
            "string",
            "null"
          ]
        },
        "priority": {
          "description": "Ascending try order. Ties break on `id` so ordering is total and a\nmerged set from several sources is deterministic.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "repository": {
          "description": "Source repository, for the forge kinds. Accepts the same spellings as\n`[package.repository].url`, including scp-style `git@host:owner/repo`.\nDefaults to the package's own declared repository.",
          "type": [
            "string",
            "null"
          ]
        },
        "serves": {
          "$ref": "#/$defs/MirrorServesV1"
        },
        "tag_template": {
          "description": "Release tag carrying this version's assets. Defaults to the package's\n`[publish].tag_format`, so mirrored artifacts hang off the exact tag\nzed already verifies for provenance.",
          "type": [
            "string",
            "null"
          ]
        },
        "url": {
          "description": "Base URL. Required for `object-store` and `zed-registry`; optional for\nthe forge kinds, where it overrides the host derived from `repository`\n(a Pages origin, or a CDN in front of raw content).",
          "type": [
            "string",
            "null"
          ]
        },
        "version_template": {
          "description": "Version-metadata key template; see [`DEFAULT_VERSION_TEMPLATE`].",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "kind"
      ]
    },
    "MirrorKindV1": {
      "description": "Transport family of a mirror. Each kind implies a URL derivation and a set\nof fields that are meaningful; [`MirrorDescriptorV1::validate`] rejects\nfields belonging to a different kind rather than ignoring them, so a\nmisplaced key is a loud error instead of a mirror that silently never\nresolves.",
      "oneOf": [
        {
          "description": "Another `zed-api-server`, spoken to with the ordinary registry API.",
          "type": "string",
          "const": "zed-registry"
        },
        {
          "description": "A content-addressed HTTP object store or CDN in front of one (R2, S3,\nMinIO). Public read, no credential, no presign: the sha256 in the path\nis both the address and the integrity check.",
          "type": "string",
          "const": "object-store"
        },
        {
          "description": "Release assets on a Git forge that serves them at a public, predictable\npath. Named for GitHub because that is the deployment that matters, but\nGitHub Enterprise, Gitea, and Forgejo use the same route shape.",
          "type": "string",
          "const": "github-release"
        },
        {
          "description": "A branch or Pages site served as raw files. Cheap and cacheable, which\nmakes it the best index transport; poor for artifact bytes, which is\nwhy `serves.artifacts` defaults off for this kind.",
          "type": "string",
          "const": "github-raw"
        },
        {
          "description": "A local directory in `file://` registry layout — an air-gapped mirror,\na warmed CI cache, or a Nix store input.",
          "type": "string",
          "const": "directory"
        },
        {
          "description": "An OCI distribution registry holding the artifact as a blob\n(`ghcr.io`, and any other registry implementing the distribution spec).\n\nAppended rather than grouped with the other forge kinds so the derived\n`Ord` on this enum keeps the meaning it had before; the wire format is\nthe kebab-case name, so ordinal position is not observable anyway.\n\nUnlike every other kind, a blob fetch needs a bearer token even when the\npackage is public: the distribution spec's anonymous flow issues one\nfrom the registry's token endpoint. The URL is still deterministic —\nzed content-addresses its artifacts, and an OCI blob is addressed by\ndigest, so the two coordinate systems are the same one.",
          "type": "string",
          "const": "oci-registry"
        }
      ]
    },
    "MirrorServesV1": {
      "description": "What a mirror is able to serve.\n\nArtifact bytes are self-verifying against the lockfile pin. Metadata and\nindexes are not, so a mirror that advertises them is asserting that it\ncarries publisher signatures; a client that cannot verify one must treat\nthe answer as absent rather than as trusted.",
      "type": "object",
      "properties": {
        "artifacts": {
          "description": "Artifact archives, addressed by sha256.",
          "type": "boolean",
          "default": true
        },
        "index": {
          "description": "Signed package-level version indexes (what makes range resolution\npossible while the registry is unreachable).",
          "type": "boolean",
          "default": true
        },
        "metadata": {
          "description": "Signed per-version metadata documents.",
          "type": "boolean",
          "default": true
        }
      }
    },
    "PublisherKeyStateV1": {
      "description": "Lifecycle of a publisher key.\n\nRotation is additive: a new key is enrolled `active` while the old one\nmoves to `retired`. Retired keys still verify — revoking them would break\nevery already-signed historical version, which is the opposite of what\nrotation is for. Compromise is expressed by `revoked`, which invalidates\npast signatures too and is deliberately a separate, louder state.",
      "oneOf": [
        {
          "description": "Signs new publications and verifies everything.",
          "type": "string",
          "const": "active"
        },
        {
          "description": "No longer signs; still verifies historical signatures.",
          "type": "string",
          "const": "retired"
        },
        {
          "description": "Compromised. Verification must fail, including for versions published\nbefore the revocation.",
          "type": "string",
          "const": "revoked"
        }
      ]
    },
    "PublisherKeyV1": {
      "description": "One publisher signing key.",
      "type": "object",
      "properties": {
        "algorithm": {
          "description": "Always `ed25519` in v1.",
          "type": "string",
          "pattern": "^ed25519$"
        },
        "enrolled_at": {
          "description": "RFC 3339 timestamp the key was enrolled.",
          "type": [
            "string",
            "null"
          ]
        },
        "key_id": {
          "description": "Short stable label, unique within the org. Names the key in\ndiagnostics and lets a signature say which key made it without\ncarrying the key itself.",
          "type": "string",
          "maxLength": 64,
          "minLength": 1,
          "pattern": "^[a-z0-9][a-z0-9._-]*$"
        },
        "public_key_multibase": {
          "description": "Multibase base58btc, `z`-prefixed.",
          "type": "string",
          "maxLength": 128,
          "minLength": 8,
          "pattern": "^z[1-9A-HJ-NP-Za-km-z]+$"
        },
        "revoked_reason": {
          "description": "Why the key was revoked. Present only for `revoked` keys, so an\noperator reading a failed verification learns the reason from the same\ndocument that caused the failure.",
          "type": [
            "string",
            "null"
          ]
        },
        "state": {
          "$ref": "#/$defs/PublisherKeyStateV1"
        }
      },
      "required": [
        "key_id",
        "algorithm",
        "public_key_multibase",
        "state"
      ]
    },
    "Vcs": {
      "description": "Version-control systems a package's source repository can live on.\n\nzed-pkg is VCS-agnostic by design: what gets installed is always a\nregistry artifact, and the VCS is where provenance (tags) is anchored.\nAuthors must create a matching tag on their declared backing repo\n(GitHub, GitLab, Bitbucket, Codeberg, SourceHut, Forgejo, Gitea, Azure\nDevOps, CodeCommit, Radicle, or self-hosted) before publishing.\n\n`jj` and Sapling are git-compatible and push to git remotes, so their\nprovenance is verified through git tags.",
      "type": "string",
      "enum": [
        "git",
        "hg",
        "jj",
        "sapling",
        "fossil",
        "pijul"
      ]
    },
    "VersionScheme": {
      "description": "How a package's `version` string (and its published tags) should be\ninterpreted. Defaults to [`VersionScheme::Semver`], which covers the vast\nmajority of modern ecosystems.",
      "oneOf": [
        {
          "description": "Semantic Versioning. `package.version` must be valid semver; ranges\n(`^1.2`, `>=0.2 <0.5`) resolve to the max satisfying stable version.",
          "type": "string",
          "const": "semver"
        },
        {
          "description": "Calendar Versioning (`2026.07.24`, `2026.07`). Normalized to a semver\ntotal order (leading zeros dropped, padded to major.minor.patch) so the\nsame range algebra applies. See [`normalize_calver`].",
          "type": "string",
          "const": "calver"
        },
        {
          "description": "Arbitrary tags (`release-candidate-1`, `legacy-api`). No range algebra:\na requirement must match a published version **exactly**.",
          "type": "string",
          "const": "opaque"
        }
      ]
    }
  }
}
