{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ChannelRoute",
  "description": "The fully resolved destination for one (host, channel, version) triple.\n\nThis is what a publisher acts on and what `zed release plan` prints: it\nalready accounts for the endpoint override, the dist-tag, and the\necosystem's version spelling, so no downstream caller re-derives any of it.",
  "type": "object",
  "properties": {
    "channel": {
      "$ref": "#/$defs/ReleaseChannel"
    },
    "dist_tag": {
      "description": "Distribution tag applied at publish time, where the host has one.",
      "type": [
        "string",
        "null"
      ]
    },
    "endpoints": {
      "$ref": "#/$defs/HostEndpoints"
    },
    "host": {
      "$ref": "#/$defs/NativeHost"
    },
    "moderated": {
      "description": "Publication completes only after human review or a merged pull request.",
      "type": "boolean"
    },
    "mutable": {
      "description": "The coordinate may be republished with different content.",
      "type": "boolean"
    },
    "protocol": {
      "$ref": "#/$defs/RegistryProtocol"
    },
    "requires_opt_in": {
      "description": "Consumers must opt in before a resolver selects this version.",
      "type": "boolean"
    },
    "version": {
      "description": "The version exactly as the host will store it.",
      "type": "string"
    }
  },
  "required": [
    "host",
    "protocol",
    "channel",
    "version",
    "endpoints",
    "requires_opt_in",
    "mutable",
    "moderated"
  ],
  "$defs": {
    "HostEndpoints": {
      "description": "The URLs a host exposes. Relative paths are templates completed by the\nclient; absolute URLs are used as-is.",
      "type": "object",
      "properties": {
        "download": {
          "description": "Base URL artifacts are downloaded from, when it differs from `index`.",
          "type": [
            "string",
            "null"
          ]
        },
        "index": {
          "description": "Base URL for version discovery.",
          "type": "string"
        },
        "publish": {
          "description": "Base URL for writes. `None` for read-only and VCS-published hosts.",
          "type": [
            "string",
            "null"
          ]
        }
      },
      "required": [
        "index"
      ]
    },
    "NativeHost": {
      "description": "A concrete registry host.\n\nOne variant per row of the ecosystem table, including the three that host\nnothing — [`NativeHost::Zig`], [`NativeHost::GoProxy`] in direct mode, and\n[`NativeHost::Vcs`] — because a release plan still has to say where those\npackages come from.",
      "oneOf": [
        {
          "description": "npmjs.com",
          "type": "string",
          "const": "npm"
        },
        {
          "description": "crates.io",
          "type": "string",
          "const": "crates-io"
        },
        {
          "description": "Maven Central via the Sonatype Central Portal.",
          "type": "string",
          "const": "maven-central"
        },
        {
          "description": "pypi.org",
          "type": "string",
          "const": "pypi"
        },
        {
          "description": "test.pypi.org — a full mirror used as a rehearsal track.",
          "type": "string",
          "const": "test-pypi"
        },
        {
          "description": "rubygems.org",
          "type": "string",
          "const": "rubygems"
        },
        {
          "description": "packagist.org",
          "type": "string",
          "const": "packagist"
        },
        {
          "description": "nuget.org",
          "type": "string",
          "const": "nuget"
        },
        {
          "description": "proxy.golang.org",
          "type": "string",
          "const": "go-proxy"
        },
        {
          "description": "conan.io/center",
          "type": "string",
          "const": "conan-center"
        },
        {
          "description": "swiftpackageindex.com",
          "type": "string",
          "const": "swift-package-index"
        },
        {
          "description": "cocoapods.org trunk",
          "type": "string",
          "const": "cocoapods"
        },
        {
          "description": "pub.dev",
          "type": "string",
          "const": "pub-dev"
        },
        {
          "description": "hex.pm",
          "type": "string",
          "const": "hex"
        },
        {
          "description": "PAUSE/CPAN, resolved through MetaCPAN.",
          "type": "string",
          "const": "cpan"
        },
        {
          "description": "cran.r-project.org",
          "type": "string",
          "const": "cran"
        },
        {
          "description": "hackage.haskell.org",
          "type": "string",
          "const": "hackage"
        },
        {
          "description": "stackage.org — curated snapshots of Hackage; resolve-only.",
          "type": "string",
          "const": "stackage"
        },
        {
          "description": "clojars.org",
          "type": "string",
          "const": "clojars"
        },
        {
          "description": "luarocks.org",
          "type": "string",
          "const": "luarocks"
        },
        {
          "description": "The Julia General registry.",
          "type": "string",
          "const": "julia-general"
        },
        {
          "description": "opam.ocaml.org",
          "type": "string",
          "const": "opam"
        },
        {
          "description": "code.dlang.org",
          "type": "string",
          "const": "dub"
        },
        {
          "description": "nimble.directory",
          "type": "string",
          "const": "nimble"
        },
        {
          "description": "shards.info",
          "type": "string",
          "const": "shards"
        },
        {
          "description": "pkgs.racket-lang.org",
          "type": "string",
          "const": "racket"
        },
        {
          "description": "powershellgallery.com",
          "type": "string",
          "const": "powershell-gallery"
        },
        {
          "description": "Zig, which has no registry: dependencies are URLs pinned by hash.",
          "type": "string",
          "const": "zig"
        },
        {
          "description": "A plain Git or Mercurial remote on GitHub, GitLab, or Bitbucket. Zed\nsupports these first-class, so a target may declare no registry at all\nand still be a complete, resolvable route.",
          "type": "string",
          "const": "vcs"
        }
      ]
    },
    "RegistryProtocol": {
      "description": "The wire protocol used to publish to and resolve from a registry.\n\nKeyed by protocol rather than by host so that one client implementation\ncovers every host that speaks it — the enterprise proxies in\n[`UniversalHost`] add hosts without adding protocols.",
      "oneOf": [
        {
          "description": "npm registry API: `PUT /{package}` with a base64 `_attachments`\ntarball, `GET /{package}` for the packument.",
          "type": "string",
          "const": "npm"
        },
        {
          "description": "crates.io API: `PUT /api/v1/crates/new` with a length-prefixed\nmetadata+tarball body, plus a sparse HTTP index.",
          "type": "string",
          "const": "cargo-sparse"
        },
        {
          "description": "Maven 2 repository layout: plain `PUT`/`GET` of\n`{group-path}/{artifact}/{version}/{artifact}-{version}.{ext}` with a\nsibling `maven-metadata.xml`. Also Clojars and every JVM proxy.",
          "type": "string",
          "const": "maven2"
        },
        {
          "description": "The Central Portal bundle upload that fronts Maven Central releases.\nDistinct from [`RegistryProtocol::Maven2`]: it takes one zipped bundle\nand returns a deployment id to poll, rather than per-file PUTs.",
          "type": "string",
          "const": "maven-central-portal"
        },
        {
          "description": "PyPI legacy upload (`POST /legacy/`, multipart, `:action=file_upload`)\npaired with a PEP 503/691 simple index for resolution.",
          "type": "string",
          "const": "pypi-legacy-upload"
        },
        {
          "description": "RubyGems API: `POST /api/v1/gems` with raw `.gem` bytes; versions from\n`/api/v1/versions/{name}.json` or the compact index.",
          "type": "string",
          "const": "rubygems-api"
        },
        {
          "description": "NuGet V3: `PUT /api/v2/package` to push, service index -> flat\ncontainer to resolve.",
          "type": "string",
          "const": "nuget-v3"
        },
        {
          "description": "Hex: `POST /api/publish` with a tarball; tarballs served from\n`repo.hex.pm`.",
          "type": "string",
          "const": "hex-api"
        },
        {
          "description": "pub.dev: an authenticated handshake returns a signed upload form, the\narchive goes to object storage, then a finalize URL is fetched.",
          "type": "string",
          "const": "pub-dev"
        },
        {
          "description": "Hackage: multipart upload to `/packages/`, with `/packages/candidates/`\nas a first-class candidate track.",
          "type": "string",
          "const": "hackage-api"
        },
        {
          "description": "LuaRocks: `POST /api/1/{api_key}/upload` with a rockspec.",
          "type": "string",
          "const": "luarocks-api"
        },
        {
          "description": "PAUSE, the CPAN upload gateway: multipart `authenquery?ACTION=add_uri`.\nResolution goes through MetaCPAN rather than PAUSE.",
          "type": "string",
          "const": "cpan-pause"
        },
        {
          "description": "CRAN's moderated web submission. Uploads are reviewed by humans, so\nthis protocol can plan and resolve but never auto-publish.",
          "type": "string",
          "const": "cran-submit"
        },
        {
          "description": "Conan v2 REST, where `user/channel` is part of the package reference.",
          "type": "string",
          "const": "conan-rest"
        },
        {
          "description": "Swift Package Registry (SE-0292): `PUT /{scope}/{name}/{version}`.",
          "type": "string",
          "const": "swift-registry"
        },
        {
          "description": "CocoaPods Trunk: `POST /api/v1/pods` with a podspec.",
          "type": "string",
          "const": "cocoapods-trunk"
        },
        {
          "description": "PowerShell Gallery, which is NuGet V2 under the hood but with its own\npush endpoint and metadata conventions.",
          "type": "string",
          "const": "powershell-gallery"
        },
        {
          "description": "Julia's General registry: a Git-backed registry updated by pull\nrequest, with package sources resolved from VCS.",
          "type": "string",
          "const": "julia-general"
        },
        {
          "description": "opam: a Git-backed repository of package definitions, updated by pull\nrequest; tarballs are fetched from the URLs those definitions carry.",
          "type": "string",
          "const": "opam-repository"
        },
        {
          "description": "A registry that stores no artifact and only records where the source\nlives — Packagist, the Swift Package Index, the DUB and Nimble\ndirectories, Shards, the Racket catalog, Go's proxy in its\ndirect-from-VCS mode. Publishing means pushing a tag and (sometimes)\npinging an update endpoint.",
          "type": "string",
          "const": "vcs-indexed"
        },
        {
          "description": "Go module proxy: `/{module}/@v/list`, `/@v/{version}.{info,mod,zip}`.\nRead-only by design; publication is a VCS tag the proxy then caches.",
          "type": "string",
          "const": "go-proxy"
        },
        {
          "description": "No registry at all. Dependencies are URLs or Git revisions checked by\nhash (Zig), so \"publish\" is a VCS tag and \"pull\" is a fetch.",
          "type": "string",
          "const": "direct-url"
        }
      ]
    },
    "ReleaseChannel": {
      "description": "A release track. `Stable` is the default everywhere; the rest are the\npre-release tracks a language ecosystem exposes, in descending maturity.",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "stable",
            "beta",
            "alpha"
          ]
        },
        {
          "description": "Release candidate: feature-complete, expected to become the release.",
          "type": "string",
          "const": "rc"
        },
        {
          "description": "Built from the tip of a branch on a schedule. Immutable per build.",
          "type": "string",
          "const": "nightly"
        },
        {
          "description": "Deliberately **mutable**: one coordinate, republished repeatedly.\nMaven `-SNAPSHOT`, Packagist `dev-*`, LuaRocks `dev`. The release\ninvariant that rejects same-version/different-content output has to be\nrelaxed here — see [`ReleaseChannel::is_mutable`].",
          "type": "string",
          "const": "snapshot"
        }
      ]
    }
  }
}
